// (c) 2013 CQZ Electronics
unit globalData;
{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils;

//Var
   // Global Vars
   //specNewSpec65       : Boolean;
   //audioComputing      : Boolean;
   //spectrumComputing65 : Boolean;
   //specMs65            : TMemoryStream;

   // The variable list above comprises those variables used in more than two
   // units (inclusive of maincode).  Addition of variables to this list should
   // be well thought out and NOT done if avoidable.

   // When translating fortran data types Integer*1 means a 8 bit Integer*2
   // means a 16 bit int and integer is 32 bit. Real is a single precision
   // float and real*8 is a double precision. Char*# is a string of # length,
   // PChar is the way to go there.
implementation
end.

